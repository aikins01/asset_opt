class ImageInfo {
  final int width;
  final int height;
  final String format;
  final bool hasAlpha;

  ImageInfo({
    required this.width,
    required this.height,
    required this.format,
    required this.hasAlpha,
  });
}
